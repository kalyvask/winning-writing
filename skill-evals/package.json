{
  "name": "skill-evals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Regression test suite for writing-related Claude skills. Catches silent regressions when skill prompts change.",
  "scripts": {
    "eval": "tsx runner.ts",
    "lint": "node lint-fixtures.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
